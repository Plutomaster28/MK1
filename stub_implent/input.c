#include "input.h"
#include <stdio.h>
#include <stdbool.h>

// Initialize the input system (stub)
void init_input() {
    // Initialize input system (dummy implementation)
    printf("Input system initialized.\n");
}

// Check if a specific key is currently pressed (stub)
bool is_key_pressed(char key) {
    // For demo purposes, we'll assume the 'A' key is always pressed
    return key == 'A';
}
